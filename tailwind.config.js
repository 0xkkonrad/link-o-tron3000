module.exports = {
    content: ['*.css, *.html, *.js', './*.html', 'single_page/*.*'], 
  theme: {
    extend: {
      animation: {
        'spin-slow': 'spin 3s linear infinite',
      }
    }
  },
  variants: {},
  plugins: [
  ]
}
